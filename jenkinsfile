pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/umamaheshy707/vpc.git', credentialsId: 'github-credentials'
            }
        }
        stage('Init') {
            steps {
                echo 'Running Init stage...'
                // Add init steps here
            }
        }
        stage('Plan') {
            steps {
                echo 'Running Plan stage...'
                // Add plan steps here
            }
        }
        stage('Apply') {
            steps {
                echo 'Running Apply stage...'
                // Add apply steps here
            }
        }
    }
}
